node_modules/
*.log
.DS_Store

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# persönliche Claude-Code-Einstellungen
.claude/settings.local.json

# Kompakt-Ausgabe (npm run build) ist eingecheckt: Copy-&-Paste-Installation ohne Node; tools/test/dist.test.js prüft Drift

# Zwischendateien des Diagramm-Builds (tools/docs/build-diagramme.mjs)
docs/diagramme/receipts/.tmp/
