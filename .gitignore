node_modules/
*.log
.DS_Store

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# persönliche Claude-Code-Einstellungen
.claude/settings.local.json

# Kompakt-Ausgabe für den Upload (npm run build)
/dist/
